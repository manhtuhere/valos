# Copy to .env for local development

DATABASE_URL=postgresql://valos:valos@localhost:5433/valos
WORKER_MODE=inline

# Required once brain.py stubs are replaced with real Claude calls
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-5

# Uncomment to tune pipeline behavior
# ROUTER_MIN_CONFIDENCE=0.75
# MAX_REVISIONS=2
# MEMWB_MIN_CONFIDENCE=0.75
# ENABLE_PERSIST_RUNS=true

# Only needed if WORKER_MODE=http
# MANUS_BASE_URL=http://localhost:8081
# OPENCLAW_BASE_URL=http://localhost:8082
